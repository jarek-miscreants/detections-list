/**
 * @generated
 * @file ElementsTabs (generated by DevLink)
 *
 * This file is generated by DevLink from a Webflow component. Manual edits are discouraged.
 *
 * @see {@link https://detections-ai-new.design.webflow.com | Source site in Webflow}
 */
"use client";

import { DEVLINK_SCOPE_CLASS } from "../devlinkScope";
import Block from "../webflow_modules/Basic/components/Block";
import DOM from "../webflow_modules/Builtin/components/DOM";
import HtmlEmbed from "../webflow_modules/Embed/components/HtmlEmbed";

/**
 * ElementsTabs
 *
 * @see {@link https://detections-ai-new.design.webflow.com | Source site in Webflow}
 */
export function ElementsTabs({
  borderAccordion = "true",
  showControls = true,
  tabButtons,
  tabContent,
  tabsSettingsAllowMultiple = "false",
  tabsSettingsAutoplayDuration = "0",
  tabsSettingsConvertAccordion = "true",
  tabsSettingsDuration = "0.2",
  tabsSettingsLoopControls = "true",
  tabsSettingsPreviewContent = "true",
  tabsSettingsSlideTabs = "true",
  variant = "Base",
}) {
  const _styleVariantMap = {
    Base: "",
    Vertical: "w-variant-3f1d271f-9297-c82e-11e6-e73d3e4adba9",
    "Vertical Even": "w-variant-65ca37fb-09ce-62e9-b46c-a5baf4671992",
  };

  const _activeStyleVariant = _styleVariantMap[variant];

  return (
    <div
      className={DEVLINK_SCOPE_CLASS}
      style={{
        display: "contents",
      }}
    >
      <Block
        className={`tabs_wrap ${_activeStyleVariant}`}
        data-allow-multiple={tabsSettingsAllowMultiple}
        data-autoplay-duration={tabsSettingsAutoplayDuration}
        data-border-accordion={borderAccordion}
        data-convert-accordion={tabsSettingsConvertAccordion}
        data-duration={tabsSettingsDuration}
        data-loop-controls={tabsSettingsLoopControls}
        data-preview-content={tabsSettingsPreviewContent}
        data-slide-tabs={tabsSettingsSlideTabs}
        data-tab={"component"}
        data-tab-component-id={""}
        tag={"div"}
      >
        <HtmlEmbed
          className={`tabs_css ${_activeStyleVariant}`}
          content={
            '<style>\n/* Config */\n[data-tab="component"] {\n  --tab-duration: 0.3s;\n  --tab-progress: 1;\n}\n\n/* CMS list flattening */\n:is([data-tab="panel-list"], [data-tab="button-list"]) > .w-dyn-list,\n:is([data-tab="panel-list"], [data-tab="button-list"]) > .w-dyn-list > *,\n:is([data-tab="panel-list"], [data-tab="button-list"]) > .w-dyn-list > * > * {\n  display: contents;\n}\n\n/* Hide all panels, show first (FOUC prevention) */\n[data-tab="panel-list"] > *:not(.w-dyn-list),\n[data-tab="panel-list"] > .w-dyn-list > * > * > * {\n  display: none;\n}\n[data-tab="panel-list"] > :first-child:not(.w-dyn-list),\n[data-tab="panel-list"] > .w-dyn-list > * > :first-child > * {\n  display: block;\n}\n\n/* Designer preview */\n.wf-design-mode [data-tab="component"][data-preview-content="true"] [data-tab="panel-list"] > *:not(.w-dyn-list),\n.wf-design-mode [data-tab="component"][data-preview-content="true"] [data-tab="panel-list"] > .w-dyn-list > * > * > * {\n  display: block;\n}\n.wf-design-mode [data-tab="button-list"] > [data-tab="button"]:first-child,\n.wf-design-mode [data-tab="button-list"] > .w-dyn-list > * > :first-child > [data-tab="button"] {\n  opacity: 1;\n}\n.wf-design-mode [data-tab="button-list"] > [data-tab="button"]:first-child [data-tab="line"],\n.wf-design-mode [data-tab="button-list"] > .w-dyn-list > * > :first-child > [data-tab="button"] [data-tab="line"] {\n  width: 100%;\n  opacity: 1;\n}\n\n/* Progress bar (autoplay) */\n[data-tab="button"].is-active [data-tab="line"] {\n  width: calc(100% * var(--tab-progress, 1));\n  opacity: 1;\n}\n\n/* Accordion content animation */\n[data-tab="panel"].is-accordion {\n  display: grid;\n  grid-template-rows: 0fr;\n  transition: grid-template-rows var(--tab-duration) ease;\n  overflow: hidden;\n}\n[data-tab="panel"].is-accordion > * {\n  overflow: hidden;\n}\n[data-tab="panel"].is-accordion.is-active {\n  grid-template-rows: 1fr;\n}\n\n/* Accordion icon rotation */\n[data-tab="icon"].is-accordion {\n  transition: transform var(--tab-duration) ease;\n}\n[data-tab="button"].is-active [data-tab="icon"].is-accordion {\n  transform: rotate(180deg);\n}\n\n/* Accordion structural */\n[data-tab="content-wrap"].is-accordion {\n  display: contents;\n}\n\n/* Button content mask */\n[data-tab="button-mask"] {\n  transition: grid-template-rows var(--tab-duration) ease;\n}\n[data-tab="button"].is-active [data-tab="button-mask"] {\n  grid-template-rows: 1fr;\n}\n/* ACCORDION BUTTON */\n[data-tab="button-accordion-mask"] {\n transition: grid-template-rows 0.3s ease;\n}\n[data-tab="button"].is-active [data-tab="button-accordion-mask"] {\ngrid-template-rows: 1fr;\n}\n\n</style>'
          }
        />
        <Block
          className={`tabs_button_wrap ${_activeStyleVariant}`}
          tag={"div"}
        >
          {tabButtons}
        </Block>
        <Block
          className={`tabs_content_wrap ${_activeStyleVariant}`}
          data-tab={"content-wrap"}
          tag={"div"}
        >
          {tabContent}
        </Block>
        {showControls ? (
          <Block
            className={`tabs_control_wrap ${_activeStyleVariant}`}
            data-tab={"control-wrap"}
            tag={"div"}
          >
            <DOM
              className={`tabs_control_button ${_activeStyleVariant}`}
              data-button={""}
              data-tab={"prev"}
              tag={"div"}
            >
              <DOM
                className={`tabs_control_text ${_activeStyleVariant}`}
                tag={"span"}
              >
                {"Previous"}
              </DOM>
            </DOM>
            <DOM
              className={`tabs_control_button ${_activeStyleVariant}`}
              data-button={""}
              data-tab={"next"}
              tag={"div"}
            >
              <DOM
                className={`tabs_control_text ${_activeStyleVariant}`}
                tag={"span"}
              >
                {"Next"}
              </DOM>
            </DOM>
          </Block>
        ) : null}
        <HtmlEmbed
          className={`tabs_js ${_activeStyleVariant}`}
          content={
            '<script>\ndocument.addEventListener("DOMContentLoaded", function () {\n document.querySelectorAll(".tabs_wrap").forEach((tabWrap, componentIndex) => {\n if (tabWrap.dataset.scriptInitialized) return;\n tabWrap.dataset.scriptInitialized = "true";\n\n // Configuration\n const loopControls = tabWrap.getAttribute("data-loop-controls") === "true";\n const slideTabs = tabWrap.getAttribute("data-slide-tabs") === "true";\n const autoplay = Number(tabWrap.getAttribute("data-autoplay-duration")) || 0;\n const duration = Number(tabWrap.getAttribute("data-duration")) || 0.3;\n const convertAccordion = tabWrap.getAttribute("data-convert-accordion") === "true";\n const allowMultiple = tabWrap.getAttribute("data-allow-multiple") === "true";\n const buttonList = tabWrap.querySelector(".tabs_button_list");\n const panelList = tabWrap.querySelector(".tabs_content_list");\n const previousButton = tabWrap.querySelector("[data-tab=\'prev\']");\n const nextButton = tabWrap.querySelector("[data-tab=\'next\']");\n\n // Early null check — prevents crash if structure is incomplete\n if (!buttonList || !panelList) {\n  console.warn("Tabs: missing .tabs_button_list or .tabs_content_list in:", tabWrap);\n  return;\n }\n\n let animating = false;\n let autoplayTl;\n\n // Accordion state\n const originalParent = buttonList.parentElement;\n const accordionItems = [];\n let isAccordion = false;\n\n // Flatten CMS dynamic list wrappers\n function initCMSList(slot) {\n  const dynList = Array.from(slot.children).find((child) => child.classList.contains("w-dyn-list"));\n  if (!dynList) return;\n  const nestedItems = dynList?.firstElementChild?.children;\n  if (!nestedItems) return;\n  const staticWrapper = [...slot.children];\n  [...nestedItems].forEach(el => el.firstElementChild && slot.appendChild(el.firstElementChild));\n  staticWrapper.forEach((el) => el.remove());\n }\n initCMSList(buttonList);\n initCMSList(panelList);\n\n const buttonItems = Array.from(buttonList.children);\n const panelItems = Array.from(panelList.children);\n\n if (!buttonItems.length || !panelItems.length) {\n  console.warn("Tabs: no button or panel items in:", tabWrap);\n  return;\n }\n\n // Initialize ARIA\n panelItems.forEach((panel) => {\n  panel.style.display = "none";\n  panel.setAttribute("role", "tabpanel");\n  panel.setAttribute("tabindex", "0");\n });\n buttonItems.forEach((button) => {\n  button.setAttribute("role", "tab");\n });\n panelList.removeAttribute("role");\n buttonList.setAttribute("role", "tablist");\n\n let activeIndex = 0;\n\n // Parse URL params once outside the button loop\n const urlParams = new URLSearchParams(location.search);\n\n // --- Tab activation ---\n const initTabActive = (index, focus = false, animate = true, pause = true) => {\n  if (animating || isAccordion) return;\n\n  buttonItems.forEach((btn, i) => {\n   btn.classList.toggle("is-active", i === index);\n   btn.setAttribute("aria-selected", i === index ? "true" : "false");\n   btn.setAttribute("tabindex", i === index ? "0" : "-1");\n  });\n  panelItems.forEach((panel, i) => panel.classList.toggle("is-active", i === index));\n\n  if (nextButton) nextButton.disabled = index === buttonItems.length - 1 && !loopControls;\n  if (previousButton) previousButton.disabled = index === 0 && !loopControls;\n  if (focus) buttonItems[index].focus();\n\n  const previousPanel = activeIndex !== index ? panelItems[activeIndex] : null;\n  const currentPanel = panelItems[index];\n  const direction = activeIndex > index ? -1 : 1;\n\n  // GSAP animations\n  if (typeof gsap !== "undefined" && animate && previousPanel) {\n   if (autoplayTl && typeof autoplayTl.restart === "function") {\n    autoplayTl.restart();\n    if (pause) {\n     autoplayTl.progress(0);\n     autoplayTl.pause();\n     gsap.set(tabWrap, { "--tab-progress": 1 });\n    }\n   }\n   animating = true;\n   const tl = gsap.timeline({\n    onComplete: () => {\n     animating = false;\n     if (typeof ScrollTrigger !== "undefined") ScrollTrigger.refresh();\n    },\n    defaults: { duration: duration, ease: "power1.out" }\n   });\n\n   if (slideTabs) {\n    tl.set(currentPanel, { display: "block", position: "relative" });\n    tl.set(previousPanel, { position: "absolute", top: 0, left: 0, width: "100%" });\n    tl.fromTo(previousPanel, { xPercent: 0 }, { xPercent: -120 * direction });\n    tl.fromTo(currentPanel, { xPercent: 120 * direction }, { xPercent: 0 }, "<");\n    tl.set(previousPanel, { display: "none" });\n   } else {\n    tl.to(previousPanel, { opacity: 0 });\n    tl.set(previousPanel, { display: "none" });\n    tl.set(currentPanel, { display: "block" });\n    tl.fromTo(currentPanel, { opacity: 0 }, { opacity: 1 });\n   }\n  } else {\n   // No GSAP or same index — just swap display\n   if (previousPanel) previousPanel.style.display = "none";\n   if (currentPanel) currentPanel.style.display = "block";\n  }\n\n  // Scroll button into view only if needed\n  const btn = buttonItems[index];\n  const listRect = buttonList.getBoundingClientRect();\n  const btnRect = btn.getBoundingClientRect();\n  if (btnRect.left < listRect.left || btnRect.right > listRect.right) {\n   buttonList.scrollTo({ left: btn.offsetLeft, behavior: "smooth" });\n  }\n\n  activeIndex = index;\n };\n\n // --- Accordion toggle ---\n const initAccordionToggle = (index) => {\n  const button = buttonItems[index];\n  const panel = panelItems[index];\n  const isActive = button.classList.contains("is-active");\n\n  if (!allowMultiple) {\n   buttonItems.forEach((btn, i) => {\n    if (i !== index) {\n     btn.classList.remove("is-active");\n     btn.setAttribute("aria-expanded", "false");\n     panelItems[i].classList.remove("is-active");\n    }\n   });\n  }\n\n  button.classList.toggle("is-active");\n  panel.classList.toggle("is-active");\n  button.setAttribute("aria-expanded", !isActive);\n\n  if (typeof ScrollTrigger !== "undefined") ScrollTrigger.refresh();\n };\n\n // --- Collect child elements that need .is-accordion combo class ---\n // These are elements inside component slots that can\'t be styled\n // via parent-dependent selectors in Webflow Designer.\n const contentWrap = tabWrap.querySelector(".tabs_content_wrap");\n const controlWrap = tabWrap.querySelector(".tabs_control_wrap");\n\n const toggleAccordionClasses = (add) => {\n  const method = add ? "add" : "remove";\n  buttonItems.forEach((btn) => {\n   btn.classList[method]("is-accordion");\n   btn.querySelectorAll(".tabs_button_line, .accordion-cms_toggle_icon").forEach(\n    (el) => el.classList[method]("is-accordion")\n   );\n  });\n  panelItems.forEach((panel) => panel.classList[method]("is-accordion"));\n  if (contentWrap) contentWrap.classList[method]("is-accordion");\n  if (controlWrap) controlWrap.classList[method]("is-accordion");\n };\n\n // --- Transform: tabs → accordion ---\n const initAccordionTransform = () => {\n  if (isAccordion) return;\n  isAccordion = true;\n  tabWrap.classList.add("is-accordion");\n  toggleAccordionClasses(true);\n\n  if (autoplayTl) {\n   autoplayTl.pause();\n   gsap.set(tabWrap, { "--tab-progress": 1 });\n  }\n\n  buttonItems.forEach((button, index) => {\n   const panel = panelItems[index];\n   const wrapper = document.createElement("div");\n   wrapper.className = "accordion_item";\n\n   const contentInner = document.createElement("div");\n   contentInner.className = "accordion_content_inner";\n\n   while (panel.firstChild) {\n    contentInner.appendChild(panel.firstChild);\n   }\n   panel.appendChild(contentInner);\n\n   // Clear inline styles from tab mode\n   panel.style.cssText = "";\n\n   const isCurrentlyActive = index === activeIndex;\n   button.classList.toggle("is-active", isCurrentlyActive);\n   panel.classList.toggle("is-active", isCurrentlyActive);\n\n   // ARIA: switch to accordion semantics\n   button.setAttribute("role", "button");\n   button.setAttribute("aria-expanded", isCurrentlyActive ? "true" : "false");\n   button.removeAttribute("aria-selected");\n   button.removeAttribute("tabindex");\n   panel.removeAttribute("role");\n\n   wrapper.appendChild(button);\n   wrapper.appendChild(panel);\n   accordionItems.push(wrapper);\n\n   if (index === 0) {\n    originalParent.appendChild(wrapper);\n   } else {\n    accordionItems[index - 1].after(wrapper);\n   }\n  });\n\n  buttonList.style.display = "none";\n  panelList.style.display = "none";\n\n  if (typeof ScrollTrigger !== "undefined") ScrollTrigger.refresh();\n };\n\n // --- Transform: accordion → tabs ---\n const initTabsTransform = () => {\n  if (!isAccordion) return;\n  isAccordion = false;\n  tabWrap.classList.remove("is-accordion");\n  toggleAccordionClasses(false);\n\n  const activeStates = buttonItems.map(btn => btn.classList.contains("is-active"));\n  const firstActiveIndex = activeStates.findIndex(state => state);\n  if (firstActiveIndex !== -1) activeIndex = firstActiveIndex;\n\n  accordionItems.forEach((wrapper, index) => {\n   const button = buttonItems[index];\n   const panel = panelItems[index];\n   const contentInner = panel.querySelector(".accordion_content_inner");\n\n   if (contentInner) {\n    while (contentInner.firstChild) {\n     panel.appendChild(contentInner.firstChild);\n    }\n    contentInner.remove();\n   }\n\n   panel.style.display = "none";\n\n   // ARIA: restore tab semantics\n   button.setAttribute("role", "tab");\n   button.setAttribute("aria-selected", "false");\n   button.setAttribute("tabindex", "-1");\n   button.removeAttribute("aria-expanded");\n   panel.setAttribute("role", "tabpanel");\n   panel.setAttribute("tabindex", "0");\n\n   buttonList.appendChild(button);\n   panelList.appendChild(panel);\n   wrapper.remove();\n  });\n\n  accordionItems.length = 0;\n  buttonList.style.display = "";\n  panelList.style.display = "";\n  animating = false;\n  initTabActive(activeIndex, false, false);\n\n  if (autoplayTl && autoplay !== 0) {\n   autoplayTl.play();\n  }\n };\n\n // --- Viewport check for accordion conversion ---\n const initViewportCheck = () => {\n  if (!convertAccordion) return;\n  const isMobile = window.innerWidth <= 767;\n  if (isMobile && !isAccordion) initAccordionTransform();\n  else if (!isMobile && isAccordion) initTabsTransform();\n };\n\n // Initialize first tab\n initTabActive(0, false, false);\n\n // Navigation helpers\n const updateIndex = (delta, focus = false, pause = true) => {\n  initTabActive((activeIndex + delta + buttonItems.length) % buttonItems.length, focus, true, pause);\n };\n\n nextButton?.addEventListener("click", () => updateIndex(1));\n previousButton?.addEventListener("click", () => updateIndex(-1));\n\n // --- Button setup: IDs, events, URL deep-linking ---\n buttonItems.forEach((btn, index) => {\n  let tabId = tabWrap.getAttribute("data-tab-component-id");\n  tabId = tabId ? tabId.toLowerCase().replace(/[^a-z0-9-]/g, "-") : String(componentIndex + 1);\n  let itemId = btn.getAttribute("data-tab-item-id");\n  itemId = itemId ? itemId.toLowerCase().replace(/[^a-z0-9-]/g, "-") : String(index + 1);\n\n  btn.setAttribute("id", "tab-button-" + tabId + "-" + itemId);\n  btn.setAttribute("aria-controls", "tab-panel-" + tabId + "-" + itemId);\n  panelItems[index].setAttribute("id", "tab-panel-" + tabId + "-" + itemId);\n  panelItems[index].setAttribute("aria-labelledby", btn.id);\n\n  // URL deep-link\n  if (urlParams.get("tab-id") === tabId + "-" + itemId) {\n   initTabActive(index);\n   tabWrap.scrollIntoView({ behavior: "smooth", block: "start" });\n   const cleanUrl = new URL(location.href);\n   cleanUrl.searchParams.delete("tab-id");\n   history.replaceState({}, "", cleanUrl);\n  }\n\n  // Click\n  btn.addEventListener("click", () => {\n   if (isAccordion) initAccordionToggle(index);\n   else initTabActive(index);\n  });\n\n  // Keyboard navigation\n  btn.addEventListener("keydown", (e) => {\n   if (isAccordion) {\n    if (e.key === "Enter" || e.key === " ") {\n     e.preventDefault();\n     initAccordionToggle(index);\n    } else if (e.key === "ArrowDown") {\n     e.preventDefault();\n     const next = buttonItems[index + 1] || buttonItems[0];\n     next.focus();\n    } else if (e.key === "ArrowUp") {\n     e.preventDefault();\n     const prev = buttonItems[index - 1] || buttonItems[buttonItems.length - 1];\n     prev.focus();\n    } else if (e.key === "Home") {\n     e.preventDefault();\n     buttonItems[0].focus();\n    } else if (e.key === "End") {\n     e.preventDefault();\n     buttonItems[buttonItems.length - 1].focus();\n    }\n   } else {\n    if (["ArrowRight", "ArrowDown"].includes(e.key)) {\n     e.preventDefault();\n     updateIndex(1, true);\n    } else if (["ArrowLeft", "ArrowUp"].includes(e.key)) {\n     e.preventDefault();\n     updateIndex(-1, true);\n    } else if (e.key === "Home") {\n     e.preventDefault();\n     initTabActive(0, true);\n    } else if (e.key === "End") {\n     e.preventDefault();\n     initTabActive(buttonItems.length - 1, true);\n    }\n   }\n  });\n });\n\n // --- Autoplay ---\n if (autoplay !== 0 && typeof gsap !== "undefined") {\n  autoplayTl = gsap.timeline({ repeat: -1 })\n   .fromTo(tabWrap, { "--tab-progress": 0 }, {\n    onComplete: () => updateIndex(1, false, false),\n    "--tab-progress": 1,\n    ease: "none",\n    duration: autoplay\n   });\n\n  let isHovered = false;\n  let hasFocusInside = false;\n  let prefersReducedMotion = false;\n  let inView = true;\n\n  function updateAuto() {\n   if (prefersReducedMotion || !inView || isHovered || hasFocusInside || isAccordion) {\n    autoplayTl.pause();\n   } else {\n    autoplayTl.play();\n   }\n  }\n\n  const motionQuery = window.matchMedia("(prefers-reduced-motion: reduce)");\n  prefersReducedMotion = motionQuery.matches;\n  motionQuery.addEventListener("change", (e) => {\n   prefersReducedMotion = e.matches;\n   updateAuto();\n  });\n\n  tabWrap.addEventListener("mouseenter", () => { isHovered = true; updateAuto(); });\n  tabWrap.addEventListener("mouseleave", () => { hasFocusInside = false; isHovered = false; updateAuto(); });\n  tabWrap.addEventListener("focusin", () => { hasFocusInside = true; updateAuto(); });\n  tabWrap.addEventListener("focusout", (e) => {\n   if (!e.relatedTarget || !tabWrap.contains(e.relatedTarget)) {\n    hasFocusInside = false;\n    updateAuto();\n   }\n  });\n\n  new IntersectionObserver((entries) => {\n   inView = entries[0].isIntersecting;\n   updateAuto();\n  }, { threshold: 0 }).observe(tabWrap);\n }\n\n // --- Responsive accordion conversion ---\n if (convertAccordion) {\n  initViewportCheck();\n  let resizeTimer;\n  window.addEventListener("resize", () => {\n   clearTimeout(resizeTimer);\n   resizeTimer = setTimeout(initViewportCheck, 250);\n  });\n }\n });\n});\n</script>'
          }
        />
      </Block>
    </div>
  );
}
